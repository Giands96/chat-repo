server:
  port: 8080

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root123}
    driver-class-name: com.postgresql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: t4J8S9TW3R7aAohz7Zv4ysUVmUsAVe0OpNnVBqtIgOj
  access-token: 900000 # 15 minutos en milisegundos
  refresh-token: 604800000 # 7 días en milisegundos
